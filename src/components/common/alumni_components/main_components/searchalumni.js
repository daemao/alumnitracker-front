import React from "react";
import { connect } from "react-redux";

const body = props =>{
  return <div>SearchALumni</div>
}
const mapStateToProps = state => {

}

export const SearchAlumni =  connect(mapStateToProps)(body);
